version: '3'

services:
  equilator_api_test:
    image: equilator_api
    container_name: equilator_api_test
    command: sh -c "coverage run --rcfile=.coveragerc -m pytest -s openapi -vv &&
                      coverage report --precision=2 --sort=cover --skip-empty --show-missing"
    # command: sh -c "pytest -s openapi/tests/test_engine"
    depends_on:
      equilator_api_db_test:
        condition:
          service_healthy
    env_file:
      - ./.env.tests.example

  equilator_api_db_test:
    image: postgres:14.2
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test_db
    # volumes:
    #   - /var/lib/postgresql/data
    healthcheck:
      test: pg_isready -U test -d test_db
      interval: 2s
      timeout: 3s
      retries: 5
